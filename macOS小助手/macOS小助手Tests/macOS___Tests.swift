//___FILEHEADER___

import XCTest
@testable import ___VARIABLE_productName;:identifier___

final class ___FILEBASENAMEASIDENTIFIER___: XCTestCase {

    override func setUpWithError() throws {
        // 在此处输入设置代码。在调用类中的每个测试方法之前调用该方法。
    }

    override func tearDownWithError() throws {
        // 在这里输入拆卸代码。该方法在调用类中的每个测试方法后调用。
    }

    func testExample() throws {
        // 这是一个功能测试用例的示例。
        // 使用XCTAssert和相关函数验证测试是否产生正确的结果。
        // 为XCTest编写的任何测试都可以注释为throws和async。
        // 当测试遇到未捕获的错误时，标记测试抛出以产生意外失败。
        // 将测试标记为异步，以允许等待异步代码完成。随后用断言检查结果。
    }

    func testPerformanceExample() throws {
        // 这是一个性能测试用例的示例。
        self.measure {
            // 把你想测量时间的代码放在这里。
        }
    }

}
